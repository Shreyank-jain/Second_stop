{
    int i,j,count;
    sort(arr,arr+n);
    sort(dep,dep+n);
    count=1;
    j=0;
    for(i=1;i<n;i++){
        
        if(dep[j]>=arr[i]){
            count++;
        }
        else {
            j++;
            
        }
    }
    return count;
   }
