bool isRotated(string str1, string str2)
    {
        string sr=str2+str2;
        string sl=str1+str1;
        if(sr.find(str1)==2 || sl.find(str2)==2)
            return 1;
        return 0;
    }
