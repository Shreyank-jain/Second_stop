void rearrange(long long *arr, int n) 
    { 
    	int start=0,end=n-1;
    	vector<int> v;
    	while(start<=end)
    	{
    	    v.push_back(arr[end]);
    	    v.push_back(arr[start]);
    	    start++;
    	    end--;
    	}
    	for(int i=0;i<n;i++)
    	    arr[i]=v[i];  	 
    }
